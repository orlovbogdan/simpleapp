<ul>
  <% pages.each do |page, children| %>
      <li>
        <%= link_to_unless_current page.name, nested_page_path(page) %>
        <%= render '_submenu_pages', pages: children if children.present? %>
      </li>
  <% end %>
</ul>